# Core (required)
pyserial
flask
numpy
# opencv: on a Pi, prefer  sudo apt install python3-opencv  (pip build is slow/heavy)
opencv-python

# Optional - AI room audit (heavy on a Pi: pulls torch). Skip if the Pi struggles.
ultralytics

# Joystick trigger now uses the ROS /joy topic (rclpy + sensor_msgs, from your ROS
# install - NOT pip). pygame is only needed for the legacy USE_JOYSTICK path, which
# you should leave OFF while a ROS joy_node owns the gamepad.
# pygame